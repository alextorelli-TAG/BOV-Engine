# Asset-class section dividers
Drop full-bleed 792x612 images here in subfolders named by asset class
(multifamily/office/retail/industrial/mixeduse/devsite). The catalog generator tags each with its assetClass so the console can auto-bind the divider when that class is selected.
